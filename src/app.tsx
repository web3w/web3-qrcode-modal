import React, {useState} from 'react';
import {createRoot} from 'react-dom/client';
// import * as QRModal from "./index"

const rootDiv = document.getElementById('root')!;
const root = createRoot(rootDiv);

// 装载
root.render(<div> ----------- </div>);
// // 卸载
// root.unmount();
